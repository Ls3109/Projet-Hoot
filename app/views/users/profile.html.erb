<div class="jumbotron">
  <h1 class="display-4">Profile</h1>
  
<div>
<h1> Email: <%= @userinfo.email %></h1>
<h2>You have tweeted <%= @usercount.size %> time(s).</h2>
</div>
 
<div>
<% @tweets.reverse.each do |tweet| %>
  <% if @userinfo == tweet.user %>
    <p>
      <tr>
        <div  class="tweet card p-3">
          <h3 class="black"><td><%= link_to tweet.user.email, user_profile_path(tweet.user) %></td></h3>
          <h2><td><%= tweet.body %></td></h2>
          <h5><td><%= tweet.created_at %></td></h5>
          <span class="twitter-button">  
            <%= button_to "Delete", tweet_path(tweet.id), :confirm => "Are you sure?", :method => :delete, class: "btn btn-danger" %> 
          </span>
        </div>
      </tr>
    </p>
  <% end %>
<% end %>
</div>
</div>

